REQUIREJS OPTIMIZER
(http://requirejs.org/)

==DOCS== 
http://requirejs.org/docs/optimization.html

==INFO==
1. Install RequireJS Optimizer 
	$ npm install -g requirejs

2. Inside the RefuelJS directory execute:
    $ r.js -o lib/require_build.js 

3. In lib/ folder you will find the minified framework